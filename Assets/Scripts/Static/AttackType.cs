namespace Static
{
    /// <summary>
    /// Defines the types of attacks that characters or entities can perform.
    /// </summary>
    public enum AttackType
    {
        /// <summary>
        /// Represents a melee attack, allowing minions to use the melee fighter interface.
        /// </summary>
        Melee,
        /// <summary>
        /// Represents a ranged attack, allowing minions to use the range fighter interface.
        /// </summary>
        Range
    }
}